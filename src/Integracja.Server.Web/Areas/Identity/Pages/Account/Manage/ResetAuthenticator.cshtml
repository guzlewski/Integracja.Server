@page
@model ResetAuthenticatorModel
@{
    ViewData["Title"] = "Reset klucza uwierzytelniającego";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h4>@ViewData["Title"]</h4>
<div class="alert alert-warning" role="alert">
    <p>
        <span class="glyphicon glyphicon-warning-sign"></span>
        <strong>Jeśli zresetujesz twój klucz uwierzytelniający twoja aplikacja uwierzytelniająca przestanie działać dopóki nie skonfigurujesz jej ponownie.</strong>
    </p>
    <p>
        Ten proces wyłącza 2FA do czasu weryfikacji twojej aplikacji uwierzytelniającej.
        Jeśli nie ukończysz konfiguracji twojej aplikacji uwierzytelniającej możesz stracić dostęp do swojego konta.
    </p>
</div>
<div>
    <form id="reset-authenticator-form" method="post" class="form-group">
        <button id="reset-authenticator-button" class="btn btn-danger" type="submit">Zresetuj klucz uwierzytelniający</button>
    </form>
</div>